server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/srt_kg_invgraph?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml

neo4j:
  uri: bolt://localhost:7687
  username: neo4j
  password: password

nlp:
  service:
    url: http://localhost:8000
